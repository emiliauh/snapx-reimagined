id: io.emiliauh.SnapXL.SnapX
runtime: org.freedesktop.Platform
runtime-version: "25.08"
sdk: org.freedesktop.Sdk
command: snapx-ui

sdk-extensions:
  - org.freedesktop.Sdk.Extension.dotnet10
  - org.freedesktop.Sdk.Extension.llvm21

finish-args:
  - --socket=wayland # Avalonia Wayland support is coming within a few months, without this socket, screenshotting on Wayland fails even though the app runs in X11.
  - --socket=x11
  - --socket=pulseaudio # SnapX plays sounds on upload
  - --device=dri # GPU support
  - --share=ipc
  - --share=network
  - --talk-name=com.canonical.AppMenu
  - --talk-name=com.canonical.dbusmenu
  - --talk-name=org.freedesktop.Notifications # Native notfications support
  - --talk-name=org.freedesktop.secrets
  - --talk-name=org.freedesktop.portal.Desktop
  - --talk-name=org.kde.kwalletd5 # OS Secret support
  - --talk-name=org.kde.StatusNotifierWatcher
  - --own-name=org.kde.* # Avalonia's Tray implementation is old, it seems? Without this, the app immediately crashes!
  - --talk-name=org.kde.StatusNotifierItem
  - --filesystem=xdg-run/keyring # OS Secret support
  - --env=LD_LIBRARY_PATH=/app/lib:/app/lib/snapx # Fixes unable to load shared library 'OpenCvSharpExtern' or one of its dependencies
modules:
  - name: snapx-ui
    buildsystem: simple
    build-options:
      append-path: /usr/lib/sdk/dotnet10/bin:/usr/lib/sdk/llvm21/bin
      append-ld-library-path: /usr/lib/sdk/dotnet10/lib
      prepend-ld-library-path: /usr/lib/sdk/llvm21/lib
      prepend-pkg-config-path: /usr/lib/sdk/dotnet10/lib/pkgconfig
      env:
        PKGTYPE: FLATPAK
    build-commands:
      - rm global.json || true
      # Forcing GitVersion to play nice with our shallow clone, otherwise it fails to determine the version number and thus fails the build.
      - git checkout -B develop
      - dotnet restore build -p:RestorePackagesPath=vendor --ignore-failed-sources --source vendor
      - dotnet run --project build --no-restore -- install --no-color --no-extended-chars --prefix ${FLATPAK_DEST} --extra-args="-p:RestorePackagesPath=vendor --source vendor --ignore-failed-sources"
      - rm ${FLATPAK_DEST}/bin/snapx ${FLATPAK_DEST}/lib/snapx/snapx # Remove duplicate CLI, available in snapx-ui binary already.
    sources:
      - type: git
        url: https://github.com/emiliauh/snapx-reimagined.git
        commit: 62a5e5b43e39a229b41a0eb54b90a05d0acd7da7
      - sources.json
      - build-sources.json
  # XRandr is a required dependency.
  - name: xrandr
    buildsystem: simple
    sources:
      - type: git
        url: https://gitlab.freedesktop.org/xorg/app/xrandr.git
        tag: xrandr-1.5.3
        commit: 0591ca56f9628d1a1a691160d79d9afb0f225c4d
    build-commands:
      - ./autogen.sh --prefix=/app
      - make -j$(nproc)
      - make install
  # For diagnostics & telemetry.
  - name: glxinfo
    buildsystem: meson
    config-opts:
      - --bindir=/app/mesa-demos
      - -Degl=disabled
      - -Dglut=disabled
      - -Dosmesa=disabled
      - -Dvulkan=disabled
      - -Dwayland=disabled
    post-install:
      - mv -v /app/mesa-demos/glxinfo /app/bin
    sources:
      - type: archive
        url: https://archive.mesa3d.org/demos/mesa-demos-9.0.0.tar.xz
        sha256: 3046a3d26a7b051af7ebdd257a5f23bfeb160cad6ed952329cdff1e9f1ed496b
        x-checker-data:
          type: anitya
          project-id: 16781
          stable-only: true
          url-template: https://archive.mesa3d.org/demos/mesa-demos-$version.tar.xz
    cleanup:
      - /include
      - /mesa-demos
      - /share
    modules:
      - name: glu
        buildsystem: meson
        sources:
          - type: archive
            url: https://archive.mesa3d.org/glu/glu-9.0.3.tar.xz
            sha256: bd43fe12f374b1192eb15fe20e45ff456b9bc26ab57f0eee919f96ca0f8a330f
        cleanup:
          - "/include"
          - "/lib/*.a"
          - "/lib/*.la"
          - "/lib/pkgconfig"

  # TLS 1.3 support
  - name: msquic
    buildsystem: cmake
    builddir: true
    sources:
      - type: git
        url: https://github.com/microsoft/msquic.git
        tag: v2.5.6
# For diagnostics & telemetry.
  - name: lspci
    buildsystem: simple
    sources:
      - type: git
        url: https://github.com/pciutils/pciutils.git
        tag: v3.14.0
    build-commands:
      - make
      - make PREFIX=/app install
